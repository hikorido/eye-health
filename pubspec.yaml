name: eye_health
description: 20-20-20 eye health reminder app.
publish_to: 'none'
version: 1.1.3+5

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^17.2.4
  shared_preferences: ^2.3.2
  usage_stats: ^1.1.0
  workmanager: ^0.9.0
  timezone: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
